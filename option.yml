dir_rule:
  base_dir: /tmp/jm_dl/
  rule: Bd_Aid

client:
  impl: api
  async_impl: async_api
  cache: level_option
  retry_times: 3
  postman:
    meta_data:
      timeout: 30

download:
  image:
    suffix: .jpg
    decode: false


